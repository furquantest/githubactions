on:
  pull_request:
    branches: [ main ]
    types:
      - opened
      - reopened
      - synchronize
jobs:
  gradle-build-test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Build with docker
        run: docker build . -t name